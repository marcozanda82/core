import React from 'react';
import BottomChrome from '../features/salaComandi/BottomChrome';

/** Barra FAB + navigazione tab inferiore (fissa sotto il contenuto autenticato). */
export default function AppBottomNavigation(props) {
  return <BottomChrome {...props} />;
}
