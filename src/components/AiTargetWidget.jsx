/**
 * @deprecated Shiftable Training Block: usa `useTrainingBlock`
 * (`src/hooks/planning/useTrainingBlock.js`). Non montare in Home.
 */
export default function AiTargetWidget() {
  if (typeof console !== 'undefined') {
    console.warn('[AiTargetWidget] Deprecato: usa useTrainingBlock.');
  }
  return null;
}
